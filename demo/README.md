# Demo

`demo/` is the parent folder for runnable demo and simulation workflows.

Current folders:
- `deadband/`: optimized deadband frequency-control demo, updated for the
  stable ANDES/AMS workflow with dispatch-to-TDS replay, repaired PV curves,
  and published reference results

Future demos should be added as new sibling subfolders under `demo/`.
